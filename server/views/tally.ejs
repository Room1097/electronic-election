<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tally Results</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 50px;
    }
    .results {
      font-size: 1.2rem;
      color: #333;
    }
    .result {
      margin-top: 10px;
    }
    a {
      color: blue;
      text-decoration: none;
      margin-top: 20px;
      display: inline-block;
    }
  </style>
</head>
<body>
  <h1>Tally Results</h1>
  <div class="results">
    <p class="result">Message: <strong><%= message %></strong></p>
    <p class="result">Product of c1 mod p: <strong><%= product_c1 %></strong></p>
    <p class="result">Product of c2 mod p: <strong><%= product_c2 %></strong></p>
    <p class="result">Data from Server 5001: <strong><%= data_from_5001 %></strong></p>
    <p class="result">Data from Server 5002: <strong><%= data_from_5002 %></strong></p>
    <p class="result">Value w1: <strong><%= w1 %></strong></p>
    <p class="result">Value w2: <strong><%= w2 %></strong></p>
    <p class="result">c1 Secret: <strong><%= c1_secret %></strong></p>
    <p class="result">Modular Inverse of c1 Secret: <strong><%= mod_inv_c1_secret %></strong></p>
    <p class="result">Decrypted Message (m): <strong><%= m %></strong></p>
    <p class="result">Exponent d: <strong><%= d %></strong></p>
  </div>
  <a href="/">Back to Home</a>
</body>
</html>
